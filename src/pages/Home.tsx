const Home = () => {

  return (
  <div>
    <h1>Home PAGE</h1>
    {/* {props.name ? 'Hi ' + props.name : 'You are not logged in'} */}
    {/* <h2>{props.name ? 'Hi ' + props.name + '!' : 'You are not logged in!'}</h2> */}

  </div>);
};

export default Home;
